#include <stdio.h>
#include <stdlib.h>

int main()
{
    int M, N;

    printf("Qual a quantidade de linhas da Matriz ? ");
    scanf("%d", &M);
    printf("Qual a quantidade de colunas da Matriz ? ");
    scanf("%d", &N);

    return 0;
}
